import { Shield, User, Lock, Circle } from 'lucide-react';

export const FallbackShieldIcon = Shield;
export const FallbackUserIcon = User;
export const FallbackLockIcon = Lock;
export const FallbackDefaultIcon = Circle;
